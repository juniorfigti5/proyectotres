<%= form_for(voice) do |f| %>
  <% if voice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(voice.errors.count, "error") %> prohibited this voice from being saved:</h2>

      <ul>
      <% voice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :contest_id, :value => params[:contest_id] %>
<form>
  <div class="form-group row">  
    <label for="inputEmail3" class="col-sm-2 col-form-label">Email*</label>
    <div class="col-sm-10">
     <%= f.text_field(:email, {:class => 'form-control'}) %><p></p>
    </div>

    <label for="inputEmail3" class="col-sm-2 col-form-label">Nombre</label>
    <div class="col-sm-10">
    <%= f.text_field(:name, {:class => 'form-control'}) %><p></p>
    </div>

    <label for="inputEmail3" class="col-sm-2 col-form-label">Surname</label>
    <div class="col-sm-10">
    <%= f.text_field(:surname, {:class => 'form-control'}) %><p></p>
  </div>

  <label for="inputEmail3" class="col-sm-2 col-form-label">Subir audio</label>
  <div class="col-sm-10">
   <%= f.file_field :original_file %><p></p>
  </div>

  <div class="actions">
    <%= f.submit("Participar", {:class => 'btn btn-success btn-lg btn-block'}) %>
  </div>

  
<% end %>

</div>
</form>